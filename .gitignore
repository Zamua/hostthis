# secrets
.env
.env.*
!.env.example

# os cruft
.DS_Store
*.swp
*.swo

# build artifacts
/bin/
/data/
/hostthis-metadata-migrate
/deploy/vps/

# locally-generated SSH host/test keys (never commit private keys)
internal/ssh/id_ed25519
internal/ssh/id_ed25519.pub

# local-dev go.work (points shale at the on-disk checkout)
go.work
go.work.sum

# prebuilt amd64 slatedb lib (operator build workaround, never committed)
libslatedb_uniffi.so
Dockerfile.slatedb.local

# built binary
/hostthisd

# site-fixture demo deps (regenerated by `make rebuild-site-fixtures`).
# The demo SOURCE and the built dist/ ARE committed; node_modules is not.
testdata/sitefixtures/*/node_modules/
testdata/sitefixtures/*/.vite/
